pages:
  tags:
    - ruby:2.3.3
  stage: deploy
  script:
    - bundle install
    - nanoc compile LANG=en_US.UTF-8
    - cp -a output/* public/
  artifacts:
    paths:
      - public
  only:
    - master    